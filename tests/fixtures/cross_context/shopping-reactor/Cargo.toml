[package]
name = "shopping-reactor"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata.hex-arch]
role = "driving-adapter"
context = "shopping"

[dependencies]
pantry-shell = { path = "../pantry-shell" }
