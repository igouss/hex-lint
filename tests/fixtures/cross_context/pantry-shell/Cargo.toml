[package]
name = "pantry-shell"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata.hex-arch]
role = "usecase"
context = "pantry"

[dependencies]
pantry-domain = { path = "../pantry-domain" }
