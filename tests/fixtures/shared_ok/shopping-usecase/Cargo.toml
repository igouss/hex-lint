[package]
name = "shopping-usecase"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata.hex-arch]
role = "usecase"
context = "shopping"

[dependencies]
contracts = { path = "../contracts" }
