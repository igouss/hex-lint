[package]
name = "pantry-usecase"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata.hex-arch]
role = "usecase"
context = "pantry"

[dependencies]
pantry-domain = { path = "../pantry-domain" }
contracts = { path = "../contracts" }
